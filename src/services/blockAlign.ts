function alignBlocks(oldBlocks: string[], newBlocks: string[]) {
    // Find the largest suffix of oldBlocks that matches a prefix of newBlocks.
    // However, the LAST element of oldBlocks may be a prefix of the FIRST matching element of newBlocks (because it streams).
} 
